# LLM 接入配置（OpenAI 兼容端点）
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini

# 可选：按当前模型/服务商显式配置。未配置时 Runtime 尝试内置能力表，未命中则使用保守默认。
# MODEL_CONTEXT_WINDOW_TOKENS=512000
# MODEL_MAX_OUTPUT_TOKENS=16384
# MODEL_CONTEXT_SAFETY_TOKENS=10240

# 可选：单次 LLM 请求总超时（毫秒），覆盖"响应头 + 正文读取"。默认 240000。
# 总超时不自动重试，避免长生成连续三遍、重复计费。
# LLM_REQUEST_TIMEOUT_MS=240000

# 可选：Host 产品历史数据库。默认 ~/.payaso/payaso.db
# PAYASO_DB_PATH=/absolute/path/to/payaso.db
